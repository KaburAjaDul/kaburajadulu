---
import VolunteerDirectoryPage from '@/components/community-system/VolunteerDirectoryPage.astro';
import { LOCALES, type Locale } from '@/i18n/constants';
export function getStaticPaths() { return LOCALES.filter((locale) => locale !== 'id').map((locale) => ({ params: { lang: locale } })); }
const locale = Astro.params.lang as Locale;
---
<VolunteerDirectoryPage locale={locale} currentPath={`/${locale}/volunteer`} />
